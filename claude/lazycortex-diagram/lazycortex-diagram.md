---
role: plugin-status
stage: patch
plugin: lazycortex-diagram
current_version: 1.0.12
published_version: 1.0.11
generated_at: 2026-05-13 18:45:00 UTC
iconize_icon: LiFileClock
iconize_color: "#86efac"
---
# lazycortex-diagram — pending changes

**Status**: changed — `plugin.json` version `1.0.12` has not been published yet (last recorded release: `1.0.11`).

## Highlights since `1.0.11`

- `lazy-diagram.install` Step 1 clarified — an `installed_plugins.json` entry scoped to a different project still counts as installed; only an empty/absent entry array aborts.
- `lazy-diagram.install` now resolves `lazycortex-core`'s `default-tiers.json` via `$LAZYCORTEX_PLUGIN_DIRS` first, falling back to the plugin cache only when the env is unset (dev-vault co-resident layouts work without any cache present). Same pattern across all `lazy-*.install` skills, per the inter-plugin boundary contract.

---
*Frontmatter + status header generated by the publish-status agent. The `## Highlights since …` body is curated by the pre-commit pipeline on each meaningful commit; treat it as LLM-maintained narrative.*
