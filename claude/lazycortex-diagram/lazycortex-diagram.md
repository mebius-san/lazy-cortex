---
role: plugin-status
stage: changed
plugin: lazycortex-diagram
current_version: 1.1.8
published_version: 1.1.7
generated_at: 2026-08-22 07:12:31 UTC
iconize_icon: LiFileClock
iconize_color: "#93c5fd"
---
# lazycortex-diagram — changed since publish

**Status**: changed — version `1.1.8` is ahead of the published `1.1.7`; the pending cycle below ships with the next publish.

## Highlights since 1.1.7
- The `/lazy-diagram.draw` caller contract no longer names the Task checklist tool. A calling skill declares one step-ledger entry per seam instead of one task, with the dispatcher's return value still supplying the outcome word.
- Mermaid 11.13 rendering fixes: `themeCSS` for `block-beta`, `rowOdd` / `rowEven` for `erd`, and the old `textPlacement` for `journey`.
- The `block-beta` contract is explicit: no edges, total `class` coverage, and a flat grid on `layout`.

---
*Frontmatter + status header generated by the publish-status agent. The `## Highlights since …` body is curated by the pre-commit pipeline on each meaningful commit; treat it as LLM-maintained narrative.*
