---
role: plugin-status
stage: minor
plugin: lazycortex-diagram
current_version: 1.1.0
published_version: 1.0.12
generated_at: 2026-06-08 07:04:08 UTC
iconize_icon: LiFileClock
iconize_color: "#fde68a"
---
# lazycortex-diagram — pending changes

**Status**: changed — `plugin.json` version `1.1.0` has not been published yet (last recorded release: `1.0.12`).

## Highlights since `1.0.12`

- `lazy-diagram.install` rule sync moved to the 3-case file-sync policy — install scope is derived from `installed_plugins.json`, drift is merged silently with a prompt only on a genuine conflict, and no-longer-shipped rules are kept in place. The per-rule install / overwrite / delete prompts are gone.

---
*Frontmatter + status header generated by the publish-status agent. The `## Highlights since …` body is curated by the pre-commit pipeline on each meaningful commit; treat it as LLM-maintained narrative.*
