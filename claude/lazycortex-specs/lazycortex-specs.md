---
role: plugin-status
stage: changed
plugin: lazycortex-specs
current_version: 3.6.0
published_version: 3.5.2
generated_at: 2026-07-29 19:03:45 UTC
iconize_icon: LiFileClock
iconize_color: "#fde68a"
---
# lazycortex-specs — changed since publish

**Status**: changed — local version `3.6.0` is ahead of the published `3.5.2` in `CHANGELOG.public.md`. Run `/pub.publish` to ship the pending changes.

## Highlights since 3.5.2

- **Cross-repo design handoff.** A spec-repo product can declare `handoff.stop_after` — its assets scaffold without post-stop docs and the gate ladder ends at that gate; a dev-repo declares `spec.imports` and pulls the approved designs by git URL via the new `/spec.import` skill (or the `spec.import-pull` daemon routine). Imported docs land read-only (`spec_imported: true`, excluded from the review loop), missing products auto-register, upstream edits after handoff surface as drift findings in `spec.doctor` — never overwritten.

---
*Frontmatter + status header generated by the publish-status agent. The `## Highlights since …` body is curated by the pre-commit pipeline on each meaningful commit; treat it as LLM-maintained narrative.*
