---
role: plugin-status
stage: minor
plugin: lazycortex-specs
current_version: 3.4.0
published_version: 3.3.0
generated_at: 2026-07-23 17:18:39 UTC
iconize_icon: LiFileClock
iconize_color: "#fde68a"
---
# lazycortex-specs — pending changes

**Status**: changed — `plugin.json` version `3.4.0` has not been published yet (last recorded release: `3.3.0`).

## Highlights since `3.3.0`

- Review classes are now shared across the whole spec vault: one `design` / `plan` / `tech` / `bug` set serves every product, so registering a second (or Nth) product no longer duplicates the class set. A product whose review experts differ still gets a per-product override that shadows the shared set for its docs only.
- `spec.install` now seeds the `spec.request-router` agent's model tier via the shared `lazy-core.agent-models-seed` primitive.

---
*Frontmatter + status header generated by the publish-status agent. The `## Highlights since …` body is curated by the pre-commit pipeline on each meaningful commit; treat it as LLM-maintained narrative.*
