---
role: plugin-status
stage: changed
plugin: lazycortex-specs
current_version: 6.3.0
published_version: 6.2.0
generated_at: 2026-08-29 10:36:51 UTC
iconize_icon: LiFileClock
iconize_color: "#fde68a"
---
# lazycortex-specs — pending changes

**Status**: changed — `plugin.json` version `6.3.0` has not been published yet (last recorded release: `6.2.0`).

## Highlights since `6.2.0`

- The project-wide vault spec became mandatory: `/lazy-spec.install` seeds a `design.md` draft at the spec content-root, `/lazy-spec.product-config` refuses to register the first product while it is absent, and `/lazy-spec.doctor` warns on a vault that lacks one — the split into products is a consequence of the repo-wide spec.
- The upstream tick stopped minting a noise commit per foreign upstream commit: a unit's `spec_upstream_revision` now moves only when the unit's own mounted subtree actually changed (the freshly synced `source/` differs from the committed bytes), so an upstream repo that is busy elsewhere leaves untouched units — and their forge links — exactly as they were.

---
*Frontmatter + status header generated by the publish-status agent. The `## Highlights since …` body is curated by the pre-commit pipeline on each meaningful commit; treat it as LLM-maintained narrative.*
