---
role: plugin-status
stage: minor
plugin: lazycortex-specs
current_version: 3.5.1
published_version: 3.4.0
generated_at: 2026-07-26 17:37:28 UTC
iconize_icon: LiFileClock
iconize_color: "#86efac"
---
# lazycortex-specs — pending changes

**Status**: changed — `plugin.json` version `3.5.1` has not been published yet (last recorded release: `3.4.0`).

## Highlights since `3.4.0`

- Design-doc skeletons redesigned: product design docs now follow Overview / Goals / Principles / Design / Behavior / Known Limitations / Boundaries; feature and change design skeletons are fixed in the templates (change uses Current State / Target State); the UI layout diagram became a conditional seam anchored at a UI subsection, and the Roadmap section is gone — folder-notes own the decomposition catalog.
- Spec diagrams now verify their own output: the product architecture seam asked the drawer for a `c4-container` kind that has no template, so the drawer refused and the diagram got composed by hand instead — losing the theme directive that keeps edge labels and lines readable. The seam now names the `architecture` kind, and each authoring skill checks the fences that actually landed on disk rather than reporting that the drawer was called.

---
*Frontmatter + status header generated by the publish-status agent. The `## Highlights since …` body is curated by the pre-commit pipeline on each meaningful commit; treat it as LLM-maintained narrative.*
