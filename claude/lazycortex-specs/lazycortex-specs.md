---
role: plugin-status
stage: changed
plugin: lazycortex-specs
current_version: 3.2.0
published_version: 3.1.1
generated_at: 2026-07-21 19:14:52 UTC
iconize_icon: LiFileClock
iconize_color: "#fde68a"
---
# lazycortex-specs — changed since publish

**Status**: changed — `plugin.json` is at `3.2.0` while `CHANGELOG.public.md` publishes `3.1.1`. Run `/pub.publish` to ship.

## Highlights since 3.1.1

- Request-handler routines (`spec.request-open`, `spec.request-apply`) now default to a 60-second scan cadence and skip Obsidian folder-notes, so the inbox's own folder-note (`requests/requests.md`) no longer re-dispatches every tick. Re-run `/spec.install` to upgrade an existing vault's routines in place.

---
*Frontmatter + status header generated by the publish-status agent. The `## Highlights since …` body is curated by the pre-commit pipeline on each meaningful commit; treat it as LLM-maintained narrative.*
