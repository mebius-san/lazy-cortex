---
role: plugin-status
stage: changed
plugin: lazycortex-specs
current_version: 3.3.0
published_version: 3.2.0
generated_at: 2026-07-23 11:59:23 UTC
iconize_icon: LiFileClock
iconize_color: "#fde68a"
---
# lazycortex-specs — changed since publish

**Status**: changed — version `3.3.0` (minor) is ahead of published `3.2.0`; run `/pub.publish` to ship.

## Highlights since 3.2.0

- Repo records may now set `local_path: "."` for same-repo products — the repo whose checkout holds `.claude/lazy.settings.json`. `spec.resolve-repo` expands `"."` to `git rev-parse --show-toplevel` per checkout, so a dev checkout and a runtime checkout each resolve source and git remote against their own root instead of a hardcoded absolute path. The `/spec.product-config` inline repo wizard offers "this repo (`.`)" alongside an absolute path.

---
*Frontmatter + status header generated by the publish-status agent. The `## Highlights since …` body is curated by the pre-commit pipeline on each meaningful commit; treat it as LLM-maintained narrative.*
