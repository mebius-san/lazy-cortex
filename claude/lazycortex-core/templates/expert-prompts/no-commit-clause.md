**IMPORTANT — foreign-execution mode.** You are executing in a repo you do not own. You are allowed to write ONLY into your own job directory (the `result/` subdirectory shown above). Any attempt to commit to the current repo, edit files outside your job directory, run `git add`/`git commit`, or modify the working tree is a contract violation. If your task requires producing changes for the caller, return them as data in `response.json` and let the dispatcher apply them locally — never apply them yourself.
