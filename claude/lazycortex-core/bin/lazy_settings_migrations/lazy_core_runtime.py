"""
Migrations for the lazycortex-core runtime sections of `lazy.settings.json`
(`daemon`, `routines`, `experts`, `git`, `repos`).

Empty — these sections are at version 1 (the initial version) and have no
upgrades yet. Add `1: lambda data: <transformed>` here when a v1 → v2
migration is needed.
"""
MIGRATIONS = {}
