---
role: plugin-status
stage: minor
plugin: lazycortex-core
current_version: 5.9.0
published_version: 5.8.3
generated_at: 2026-07-06 10:26:59 UTC
iconize_icon: LiFileClock
iconize_color: "#fde68a"
---
# lazycortex-core — minor bump pending publish

**Status**: changed — version `5.9.0` is a minor step ahead of the published `5.8.3`; the pending work ships on the next `/pub.publish`.

## Highlights since 5.8.3

- The daemon can now trigger operator automation the moment it lands commits on the remote: set `daemon.git.post_push_hook` to any shell command (for example, poke a device into `git pull`) and it runs after every push that actually advances the branch, with the push context in `LAZY_PUSH_*` env variables — crash-isolated, so a failing or hanging hook never disturbs the daemon.
- The default agent-tier table now covers the new `lazy-experts.fiction-writer` agent (tier `opus`), so `/lazy-experts.install` seeds its model routing out of the box.

---
*Frontmatter + status header generated by the publish-status agent. The `## Highlights since …` body is curated by the pre-commit pipeline on each meaningful commit; treat it as LLM-maintained narrative.*
