---
role: plugin-status
stage: minor
plugin: lazycortex-core
current_version: 5.17.0
published_version: 5.16.3
generated_at: 2026-07-23 18:50:45 UTC
iconize_icon: LiFileClock
iconize_color: "#fde68a"
---
# lazycortex-core — pending changes

**Status**: changed — `plugin.json` version `5.17.0` has not been published yet (last recorded release: `5.16.3`).

## Highlights since `5.16.3`

- New shared install-helper `lazy-core.agent-models-seed`: any plugin's install now seeds its subagents' model tiers from the canonical `default-tiers.json` via a single Skill dispatch, instead of hand-rolling the lookup. `default-tiers.json` now also lists the diagram / python / specs agents, and core's own install seeds every `lazycortex-core:*` tier (previously only the runtime doctor).

---
*Frontmatter + status header generated by the publish-status agent. The `## Highlights since …` body is curated by the pre-commit pipeline on each meaningful commit; treat it as LLM-maintained narrative.*
