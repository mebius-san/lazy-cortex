---
role: plugin-status
stage: patch
plugin: lazycortex-core
current_version: 5.8.1
published_version: 5.8.0
generated_at: 2026-07-04 14:57:40 UTC
iconize_icon: LiFileClock
iconize_color: "#93c5fd"
---
# lazycortex-core — pending changes

**Status**: changed — `plugin.json` version `5.8.1` has not been published yet (last recorded release: `5.8.0`).

## Highlights since `5.8.0`

- Removed the `--bare` opt-in that had shipped in 5.8.0. Expert spawns keep only the `--setting-sources project,local` hermetic default (and its explicit per-expert opt-in) — the minimal-mode / API-key-only path is gone entirely.

---
*Frontmatter + status header generated by the publish-status agent. The `## Highlights since …` body is curated by the pre-commit pipeline on each meaningful commit; treat it as LLM-maintained narrative.*
