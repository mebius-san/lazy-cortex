---
role: plugin-status
stage: patch
plugin: lazycortex-core
current_version: 3.1.1
published_version: 3.1.0
generated_at: 2026-05-13 15:59:43 UTC
iconize_icon: LiFileClock
iconize_color: "#93c5fd"
---
# lazycortex-core — pending changes

**Status**: changed — `plugin.json` version `3.1.1` has not been published yet (last release: `3.1.0`).

## Highlights since `3.1.0`

- `/lazy-core.setup` now auto-migrates `lazy.settings.json` to the current section schemas before running per-plugin installers — the migration ladder runs transparently after every `/plugin update`, so consumer config stays current without a manual step when a section's schema bumps.

---
*Frontmatter + status header generated by the publish-status agent. The `## Highlights since …` body is curated by the pre-commit pipeline on each meaningful commit; treat it as LLM-maintained narrative.*
