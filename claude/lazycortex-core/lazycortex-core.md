---
role: plugin-status
stage: minor
plugin: lazycortex-core
current_version: 5.20.0
published_version: 5.19.1
generated_at: 2026-07-30 16:17:05 UTC
iconize_icon: LiFileClock
iconize_color: "#fde68a"
---
# lazycortex-core — pending changes

**Status**: changed — `plugin.json` version `5.20.0` has not been published yet (last recorded release: `5.19.1`).

## Highlights since `5.19.1`

- A repository can now declare working directories it does not carry in git — bulk data, an inbox, secrets. Install asks each checkout once where they live on this machine and restores the links; audit and doctor report one that stopped resolving and offer to re-link it; a routine whose declared inbox is broken now fails loudly instead of idling. Operator content sitting in a declared slot is always reported, never removed, and an inbox two checkouts would both drive is refused rather than imported twice.

---
*Frontmatter + status header generated by the publish-status agent. The `## Highlights since …` body is curated by the pre-commit pipeline on each meaningful commit; treat it as LLM-maintained narrative.*
