---
role: plugin-status
stage: major
plugin: lazycortex-core
current_version: 7.0.0
published_version: 6.2.1
generated_at: 2026-08-19 15:14:05 UTC
iconize_icon: LiFileClock
iconize_color: "#fca5a5"
---
# lazycortex-core — pending changes

**Status**: changed — `plugin.json` version `7.0.0` has not been published yet (last recorded release: `6.2.1`).

## Highlights since `6.2.1`

- The six heaviest rule files slimmed down without losing a single norm: the four always-loaded ones (`hygiene`, `git`, `guard.security`, `log.logging`) shed framing and duplicate rationale that every consumer session paid for on every turn, and the two authoring contracts (`skill-writing`, `hook-writing`) dropped a duplicated re-anchoring explanation, a dated incident post-mortem the rules themselves forbid, and self-restating reference lines — `hook-writing` is back inside its 10 KB budget.
- Shipped sources no longer point at dev-vault-only artifacts: prose and comments that named internal rules a consumer never receives now state the contract in their own words, so nothing in the shipped tree references something that does not exist on the consumer's machine.
- The settings auto-migration learned the document-type rename: review classes still named `dev-plan` / `dev-report` are renamed to `code-plan` / `code-report` with their path globs rewritten (stale globs are fixed even on already-renamed classes), and a history block naming the retired `review.historian` expert is dropped while any operator-chosen historian is preserved.
- Attaching optional protocols to a routine is the operator's own move now: `lazy-routine.offer-protocols` is invoked directly, never dispatched from a plugin's install — a system routine's protocol set is fixed by design and seeded without questions.
- The settings ladder learned the `wiki` section: v1 → v2 hoists the classification-axis vocabulary from the individual scopes to the section as the union of what the scopes declared — each scope's own list stays behind as the narrowing it now means, so no scope's effective axes change — and seeds the section-level exclude list with the project-structure map while stripping the per-scope copies of that same entry. A list the operator already reshaped is never rebuilt.

---
*Frontmatter + status header generated by the publish-status agent. The `## Highlights since …` body is curated by the pre-commit pipeline on each meaningful commit; treat it as LLM-maintained narrative.*
