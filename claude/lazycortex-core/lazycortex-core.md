---
role: plugin-status
stage: minor
plugin: lazycortex-core
current_version: 5.8.0
published_version: 5.7.0
generated_at: 2026-07-04 13:52:55 UTC
iconize_icon: LiFileClock
iconize_color: "#fde68a"
---
# lazycortex-core — pending changes

**Status**: changed — `plugin.json` version `5.8.0` has not been published yet (last recorded release: `5.7.0`).

## Highlights since `5.7.0`

- Expert spawns are now hermetic on settings scope too: they default to `--setting-sources project,local`, so a headless job no longer loads the operator's user-scope plugins or their hooks (an interactive one could otherwise stall every tool call). An expert opts back into `user` scope, or into fully minimal `--bare` mode, per entry — with the launchability preflight reporting the effective scopes and warning when `bare` has no API-key credential.

---
*Frontmatter + status header generated by the publish-status agent. The `## Highlights since …` body is curated by the pre-commit pipeline on each meaningful commit; treat it as LLM-maintained narrative.*
