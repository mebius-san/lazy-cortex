---
role: plugin-status
stage: minor
plugin: lazycortex-core
current_version: 6.2.0
published_version: 6.1.0
generated_at: 2026-08-06 20:32:14 UTC
iconize_icon: LiFileClock
iconize_color: "#fde68a"
---
# lazycortex-core — pending changes

**Status**: changed — `plugin.json` version `6.2.0` has not been published yet (last recorded release: `6.1.0`).

## Highlights since `6.1.0`

- `lazy-core.audit` and `lazy-core.doctor` now judge **command** descriptions alongside skills and agents — a command is routed by its `description:` exactly as a skill is, and the `<ns>.help` commands are the ones an operator most needs the router to find. Doctor's Phase 3 delegation list grew the two plugin audits it had never been told to run.
- `lazy-log.bullets` now judges a commit by its diff inside the plugin being released rather than by the commit subject's scope, so a repo-wide commit no longer produces an empty release block for one plugin while its siblings, changed by the very same commit, each get a bullet.

---
*Frontmatter + status header generated by the publish-status agent. The `## Highlights since …` body is curated by the pre-commit pipeline on each meaningful commit; treat it as LLM-maintained narrative.*
