---
role: plugin-status
stage: changed
plugin: lazycortex-core
current_version: 9.1.1
published_version: 9.1.0
generated_at: 2026-09-06 07:24:57 UTC
iconize_icon: LiFileClock
iconize_color: "#93c5fd"
---
# lazycortex-core — changed since 9.1.0

**Status**: changed — version `9.1.1` is ahead of the published `9.1.0`; commits have landed under `claude/lazycortex-core/` since the last publish.

## Highlights since 9.1.0

- A routine's commits are pushed right after the routine finishes instead of once per tick, so a rebase conflict in a later routine discards only its own work. When a tick is discarded, the daemon takes the `CONSUMED` marker back off every expert job consumed since the last publish (tracked in `.runtime/consumed-unpushed.log`) so the collector lands the result again, and rewinds every git-watch cursor the discarded range held to the merge-base so the routine rescans from the last published commit.
- The pump strips `ANTHROPIC_API_KEY` from expert spawns, the providers skill no longer echoes token values, and the doctor checks a configured provider endpoint answers.

---
*Frontmatter + status header generated by the publish-status agent. The `## Highlights since …` body is curated by the pre-commit pipeline on each meaningful commit; treat it as LLM-maintained narrative.*
