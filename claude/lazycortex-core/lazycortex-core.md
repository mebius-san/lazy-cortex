---
role: plugin-status
stage: minor
plugin: lazycortex-core
current_version: 5.10.0
published_version: 5.9.0
generated_at: 2026-07-07 19:28:42 UTC
iconize_icon: LiFileClock
iconize_color: "#fde68a"
---
# lazycortex-core — pending changes

**Status**: changed — `plugin.json` version `5.10.0` has not been published yet (last recorded release: `5.9.0`).

## Highlights since `5.9.0`

- **New `detect-scope` subcommand.** `lazycortex-core detect-scope <plugin@marketplace>` resolves a plugin's install target from where it is actually *enabled* — the project's `enabledPlugins` first, then the user's — and falls back to the install record's own `scope` only when neither settings file enables it. Config now follows activation rather than wherever `/plugin install` last ran. Sibling install skills call it as the boundary-legal way to detect their own scope without importing core Python.

---
*Frontmatter + status header generated by the publish-status agent. The `## Highlights since …` body is curated by the pre-commit pipeline on each meaningful commit; treat it as LLM-maintained narrative.*
