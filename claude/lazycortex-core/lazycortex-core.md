---
role: plugin-status
stage: changed
plugin: lazycortex-core
current_version: 5.13.0
published_version: 5.12.0
generated_at: 2026-07-12 21:37:22 UTC
iconize_icon: LiFileClock
iconize_color: "#fde68a"
---
# lazycortex-core — changed

**Status**: changed — version `5.13.0` is ahead of published `5.12.0`; unpublished work is pending the next `/pub.publish` run.

## Highlights since 5.12.0

- The agent-model wizard now cleans up after deleted agents: entries in `agent_models` that point at a plugin agent whose file no longer ships are pruned automatically (interactively and in headless autosetup/autocheckup runs alike), so stale tiers stop accumulating in project settings as plugins evolve. Entries it cannot prove dead — built-ins, project-authored agents, plugins not installed locally — are never touched, and an expert still referencing a deleted agent is surfaced as a warning instead of being silently edited.

---
*Frontmatter + status header generated by the publish-status agent. The `## Highlights since …` body is curated by the pre-commit pipeline on each meaningful commit; treat it as LLM-maintained narrative.*
