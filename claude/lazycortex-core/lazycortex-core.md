---
role: plugin-status
stage: minor
plugin: lazycortex-core
current_version: 6.1.0
published_version: 6.0.0
generated_at: 2026-08-06 20:10:00 UTC
iconize_icon: LiFileClock
iconize_color: "#fde68a"
---
# lazycortex-core — pending changes

**Status**: changed — `plugin.json` version `6.1.0` has not been published yet (last recorded release: `6.0.0`).

## Highlights since `6.0.0`

- Every skill and agent description now opens with the occasion that should fire it rather than a summary of internals, and `lazy-core.skill-writing` § 8 plus a new `description-triggers` reference make that the authoring contract; `lazy-core.audit` and `lazy-core.doctor` flag a description the router cannot select, and the skill / agent / command templates ship the trigger shapes in their placeholders.
- `lazy-core.doctor` also picked up the two plugin audits it had never been told to run — `lazy-obsidian.audit` and `lazy-python.audit` now surface alongside the others.
- Doctor no longer just reports a description the router cannot select: it reads the artifact and its callers, proposes a replacement line, and rewrites it on confirmation — one finding at a time, never under "apply all", and never touching anything but that one frontmatter line.

---
*Frontmatter + status header generated by the publish-status agent. The `## Highlights since …` body is curated by the pre-commit pipeline on each meaningful commit; treat it as LLM-maintained narrative.*
