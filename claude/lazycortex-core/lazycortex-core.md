---
role: plugin-status
stage: changed
plugin: lazycortex-core
current_version: 8.1.0
published_version: 8.0.1
generated_at: 2026-08-30 16:01:58 UTC
iconize_icon: LiFileClock
iconize_color: "#fde68a"
---
# lazycortex-core — pending changes

**Status**: changed — `plugin.json` version `8.1.0` has not been published yet (last recorded release: `8.0.1`).

## Highlights since `8.0.1`

- A `.git/index` displaced by a cloud-sync conflict resolution now heals itself: the new `lazy-core.index-guard` built-in routine (and a pre-flight in the git-guard hook and the daemon's git flow) restores the newest `index (…conflicted copy…)` — the version git actually wrote last — and clears the copy litter, while the daemon and manual tick run all git with `GIT_OPTIONAL_LOCKS=0` so background `status` runs stop rewriting the shared index at all.

---
*Frontmatter + status header generated by the publish-status agent. The `## Highlights since …` body is curated by the pre-commit pipeline on each meaningful commit; treat it as LLM-maintained narrative.*
