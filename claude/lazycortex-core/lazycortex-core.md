---
role: plugin-status
stage: patch
plugin: lazycortex-core
current_version: 5.17.1
published_version: 5.17.0
generated_at: 2026-07-25 09:17:12 UTC
iconize_icon: LiFileClock
iconize_color: "#93c5fd"
---
# lazycortex-core — pending changes

**Status**: changed — `plugin.json` version `5.17.1` has not been published yet (last recorded release: `5.17.0`).

## Highlights since `5.17.0`

- Per-expert job observability: the expert pump now appends every job attempt (done / failed / dead / retryable error, with wall-clock duration) to `.logs/lazy-core/runtime/jobs.jsonl`, and the daemon folds that log into two new Prometheus series — `lazycortex_runtime_expert_jobs_total{expert,outcome}` and `lazycortex_runtime_expert_job_duration_seconds{expert}` — so expert health is visible per expert, not just per routine.

---
*Frontmatter + status header generated by the publish-status agent. The `## Highlights since …` body is curated by the pre-commit pipeline on each meaningful commit; treat it as LLM-maintained narrative.*
