---
role: plugin-status
stage: changed
plugin: lazycortex-core
current_version: 5.8.2
published_version: 5.8.1
generated_at: 2026-07-04 17:47:16 UTC
iconize_icon: LiFileClock
iconize_color: "#93c5fd"
---
# lazycortex-core — unreleased changes

**Status**: changed — version `5.8.2` is ahead of the published `5.8.1`; commits have landed under `claude/lazycortex-core/` that are not yet in `CHANGELOG.public.md`.

## Highlights since 5.8.1

- Lazycortex hooks now no-op inside expert spawns by default: the pump exports a `LAZYCORTEX_HOOKS_ALLOW_LIST` marker and every core hook gates on it, so an expert runs no hook unless its `experts[<name>].hooks.enabled` list opts one back in — dropping the ~40s check-public / git-guard tax per expert Bash call. The same gate powers an interactive `hooks.disabled` block-list so an operator can silence a hook for their own sessions.

---
*Frontmatter + status header generated by the publish-status agent. The `## Highlights since …` body is curated by the pre-commit pipeline on each meaningful commit; treat it as LLM-maintained narrative.*
