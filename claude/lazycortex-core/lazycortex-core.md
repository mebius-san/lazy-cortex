---
role: plugin-status
stage: changed
plugin: lazycortex-core
current_version: 9.1.0
published_version: 9.0.1
generated_at: 2026-09-05 UTC
iconize_icon: LiFileClock
iconize_color: "#fde68a"
---
# lazycortex-core — changed since publish

**Status**: changed — version `9.1.0` is ahead of published `9.0.1`; unpublished commits sit under `claude/lazycortex-core/`.

## Highlights since 9.0.1

- Edit markers annotate changes to existing text only: the first content written into an empty section (one holding just its template comment) is emitted bare, the same way a wholly new section is, so live content downstream parses from the first round.
- The markdown-style reference now owns the link display-text rule (it moved out of the tech-writing aspect, which carries prose discipline rather than markup), and the recommendation-marker section no longer states the same rule twice for callouts and for standalone tick-lists.
- Expert jobs can now run on alternative LLM providers: a machine-local providers registry (base URL, token variable, per-tier model map) plus a per-expert `provider` key route the spawn to Kimi or OpenAI through an Anthropic-compatible endpoint, with validation before dispatch, the operator wizard `/lazy-core.providers`, install seeding, and doctor schema checks.

---
*Frontmatter + status header generated by the publish-status agent. The `## Highlights since …` body is curated by the pre-commit pipeline on each meaningful commit; treat it as LLM-maintained narrative.*
