---
role: plugin-status
stage: minor
plugin: lazycortex-core
current_version: 5.3.0
published_version: 5.2.0
generated_at: 2026-06-10 17:19:54 UTC
iconize_icon: LiFileClock
iconize_color: "#fde68a"
---
# lazycortex-core — pending changes

**Status**: changed — `plugin.json` version `5.3.0` has not been published yet (last recorded release: `5.2.0`).

## Highlights since `5.2.0`

- Install correctness fixes to the daemon-gating model. Experts, `agent_models` tiers, and the `.memory/` dir are no longer gated on `daemon.enabled` — they are dispatch-routing config used by interactive flows (the lazy-review writer-loop, manual `dispatch-job`) too, so `lazy-core.install` registers them (Steps 10.5 + 11) regardless of the daemon gate; only routines / supervisor / sandbox / runtime-plumbing stay gated.
- Gate 2 (`daemon.run_here`) is now correctly **per-checkout**, not per-machine: it lives in this working copy's own gitignored `lazy.settings.local.json`, so several checkouts of one project on a machine each decide independently. The supervisor unit is named `com.lazycortex.runtime.<basename>-<sha256(abs-path)[:8]>` so two same-named checkouts no longer collide on their launchd/systemd unit, and Step 13b/13c migrate a legacy basename-only unit that points at this checkout.
- The two install gates are reworded to be unmistakable: Gate 1 (`daemon.enabled`, header `Use daemon?`) is a project-wide policy question carrying no "run"/"here" language; Gate 2 (`daemon.run_here`, header `Run here?`) is the operational "start it on this checkout now" question.

---
*Frontmatter + status header generated by the publish-status agent. The `## Highlights since …` body is curated by the pre-commit pipeline on each meaningful commit; treat it as LLM-maintained narrative.*
