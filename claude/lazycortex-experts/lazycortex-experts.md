---
role: plugin-status
stage: minor
plugin: lazycortex-experts
current_version: 0.2.0
published_version: 0.1.0
generated_at: 2026-05-15 19:51:19 UTC
iconize_icon: LiFileClock
iconize_color: "#86efac"
---
# lazycortex-experts — pending changes

**Status**: changed — `plugin.json` version `0.2.0` has not been published yet (last recorded release: `0.1.0`). Minor cycle — consumers must re-run `/lazy-experts.install` so the new composed expert entries land in `lazy.settings.json[experts]`.

## Highlights since `0.1.0`

- `/lazy-experts.install` now seeds composed expert entries in `lazy.settings.json[experts]` in addition to agent-model tiers. For each (agent × domain-aspect) pair it writes one entry — `claude-plugin-interpreter`, `claude-plugin-designer`, `claude-plugin-planner`, `game-interpreter`, `game-designer`, `game-planner`, `dotfiles-interpreter`, `dotfiles-designer`, `dotfiles-planner` — and attaches `lazycortex-core:lazy-memory.persona-aspect` to every seeded expert so each accumulates private memory under `.memory/<self>/` across runs. Idempotent: existing entries are never overwritten, so hand-customized experts survive re-run. Adding new agents or aspects in later releases extends the cartesian product automatically — no skill edit needed.

---
*Frontmatter + status header generated by the publish-status agent. The `## Highlights since …` body is curated by the pre-commit pipeline on each meaningful commit; treat it as LLM-maintained narrative.*
