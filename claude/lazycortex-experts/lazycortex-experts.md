---
role: plugin-status
stage: major
plugin: lazycortex-experts
current_version: 1.0.0
published_version: 0.7.4
generated_at: 2026-08-20 13:52:12 UTC
iconize_icon: LiFileClock
iconize_color: "#fca5a5"
---
# lazycortex-experts — pending changes

**Status**: changed — `plugin.json` version `1.0.0` has not been published yet (last recorded release: `0.7.4`).

## Highlights since `0.7.4`

- The install skill's sibling-plugin completeness check stopped demanding the retired `review.historian` expert: the review plugin's system expert is `review.doc_doctor` alone now, and every help chapter and naming example speaks of live experts only.
- The designer gained a hard principle: a design document carries no concrete balance values — retry budgets, limits, thresholds, durations stay out of design prose and especially out of overview and summary lines; the mechanic is described by its shape while the number lives in configuration, the tech doc, or the owning code.
- A docs-writer role joined the agent set: it takes one asset's approved design and writes the product's user-facing documentation straight from it — no plan document in between — journaling what landed where into the asset's `docs-report`. Install now counts it among the branch-workspace roles, since its acceptance cycle runs continuations on the same job branch the other report-class writers use.
- The role vocabulary was recomposed: the implementer role is now `developer` and data-implementer is `data-writer` (the agents keep their filenames — install maps each role to its agent), and a new `system-designer` role, served by the designer agent, writes product- and project-level design documents. Seeded expert keys, the branch-workspace list, and the commit-rights list all follow the new names.

---
*Frontmatter + status header generated by the publish-status agent. The `## Highlights since …` body is curated by the pre-commit pipeline on each meaningful commit; treat it as LLM-maintained narrative.*
