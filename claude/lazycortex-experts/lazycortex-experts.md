---
role: plugin-status
stage: patch
plugin: lazycortex-experts
current_version: 0.2.1
published_version: 0.2.0
generated_at: 2026-05-23 13:17:18 UTC
iconize_icon: LiFileClock
iconize_color: "#86efac"
---
# lazycortex-experts — pending changes

**Status**: changed — `plugin.json` version `0.2.1` has not been published yet (last recorded release: `0.2.0`).

## Highlights since `0.2.0`

- `lazy-experts.install` resolves `lazycortex-core`'s `default-tiers.json` via `$LAZYCORTEX_PLUGIN_DIRS` first, falling back to the plugin cache only when the env is unset. Same env-first pattern across every `lazy-*.install` skill now, per the inter-plugin boundary contract.
- Three generic agents (interpreter / designer / planner) lose their inline `## Aspect awareness` and `## Protocol awareness` sections — the same boilerplate was duplicated across all three. The mechanism (every spawn gets `- protocol: <path>` / `- aspect: <path>` lines in its prompt and reads them) is now described once in `lazycortex-core`'s `expert-runtime-contract.md` that every expert spawn loads. Agent bodies are persona-only again.

---
*Frontmatter + status header generated by the publish-status agent. The `## Highlights since …` body is curated by the pre-commit pipeline on each meaningful commit; treat it as LLM-maintained narrative.*
