---
role: plugin-status
stage: minor
plugin: lazycortex-experts
current_version: 0.1.0
published_version: unpublished
generated_at: 2026-05-15 14:04:52 UTC
iconize_icon: LiFileClock
iconize_color: "#86efac"
---
# lazycortex-experts — pending changes

**Status**: changed — `plugin.json` version `0.1.0` has not been published yet (initial release). Consumers enable the plugin and run `/lazy-experts.install` to seed agent-model tiers from `lazycortex-core`'s defaults.

## Highlights since unpublished

- Initial release. Three persona-only generic agents (`lazy-experts.interpreter`, `lazy-experts.designer`, `lazy-experts.planner`) any routine can dispatch — protocol comes from the dispatching routine, not from this plugin.
- Three composable domain aspects (`lazy-experts.claude-plugin-aspect`, `lazy-experts.game-dev-aspect`, `lazy-experts.dotfiles-aspect`) layer on top of any generic agent via `lazy.settings.json[experts][<expert>].aspects[]`. Pure prompt layers — no side-effect carve-outs, no new universal kinds.
- `/lazy-experts.install` seeds `agent_models.lazycortex` from `lazycortex-core`'s `default-tiers.json` (designer defaults `opus`, interpreter and planner default `sonnet`); requires core ≥ 3.2.0.
- `/lazy-experts.help` lists every shipped agent, aspect, skill, and command verbatim.
- Building-block scope: no routines, no manual dispatcher, no protocol files ship from this plugin. Compose specialists (`game-designer`, `claude-plugin-planner`, etc.) in `lazy.settings.json[experts]` and wire the routine that dispatches them elsewhere.

---
*Frontmatter + status header generated by the publish-status agent. The `## Highlights since …` body is curated by the pre-commit pipeline on each meaningful commit; treat it as LLM-maintained narrative.*
