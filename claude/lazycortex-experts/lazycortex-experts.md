---
role: plugin-status
stage: minor
plugin: lazycortex-experts
current_version: 0.3.0
published_version: 0.2.1
generated_at: 2026-06-09 20:00:31 UTC
iconize_icon: LiFileClock
iconize_color: "#fde68a"
---
# lazycortex-experts — pending changes

**Status**: changed — `plugin.json` version `0.3.0` has not been published yet (last recorded release: `0.2.1`).

## Highlights since `0.2.1`

- `lazy-experts.install` seeds two things into `lazy.settings.json` — agent-model tiers for the three generic agents (interpreter, designer, planner) and composed expert entries (agent × domain-aspect, each carrying a persona aspect + a deterministic bot `git_author`). Registration is **not** gated on the daemon: experts and tiers are dispatch-routing config used by interactive flows too, so an enabled plugin always registers them. Expert-class selection is asked only when the experts list is still empty; on a populated list the install derives the present classes and completes silently. Install scope is derived, never asked, and re-runs are idempotent — existing entries are never overwritten.

---
*Frontmatter + status header generated by the publish-status agent. The `## Highlights since …` body is curated by the pre-commit pipeline on each meaningful commit; treat it as LLM-maintained narrative.*
