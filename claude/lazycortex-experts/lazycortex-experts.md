---
role: plugin-status
stage: patch
plugin: lazycortex-experts
current_version: 0.5.1
published_version: 0.5.0
generated_at: 2026-07-07 19:28:42 UTC
iconize_icon: LiFileClock
iconize_color: "#93c5fd"
---
# lazycortex-experts — pending changes

**Status**: changed — `plugin.json` version `0.5.1` has not been published yet (last recorded release: `0.5.0`).

## Highlights since `0.5.0`

- **Install scope now follows enablement.** The install skill resolves its target scope via `lazycortex-core detect-scope` — keyed on where the plugin is actually enabled (`enabledPlugins`) rather than the install record's own `scope` field, which drifts from the activation scope.

---
*Frontmatter + status header generated by the publish-status agent. The `## Highlights since …` body is curated by the pre-commit pipeline on each meaningful commit; treat it as LLM-maintained narrative.*
