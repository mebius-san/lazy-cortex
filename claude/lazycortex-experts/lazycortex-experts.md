---
role: plugin-status
stage: patch
plugin: lazycortex-experts
current_version: 0.7.2
published_version: 0.7.1
generated_at: 2026-08-02 21:18:20 UTC
iconize_icon: LiFileClock
iconize_color: "#86efac"
---
# lazycortex-experts — patch bump pending publish

**Status**: changed — version `0.7.2` is ahead of the published `0.7.1` at the top of `CHANGELOG.public.md`. Drop-in for consumers; no install step required.

## Highlights since 0.7.1

- The implementer now finishes a task only once the repository's own tooling agrees: its declared check runner, every test suite the repo exposes, and the guideline-review agent its language plugin ships. A repo that declares a local runner gets that runner — never the generic wrapper, never the raw tools underneath it — and relaxing a check to reach green is spelled out as a contract breach rather than a fix.

---
*Frontmatter + status header generated by the publish-status agent. The `## Highlights since …` body is curated by the pre-commit pipeline on each meaningful commit; treat it as LLM-maintained narrative.*
