---
role: plugin-status
stage: patch
plugin: lazycortex-obsidian
current_version: 2.1.1
published_version: 2.1.0
generated_at: 2026-05-13 18:45:00 UTC
iconize_icon: LiFileClock
iconize_color: "#86efac"
---
# lazycortex-obsidian — pending changes

**Status**: changed — `plugin.json` version `2.1.1` has not been published yet (last recorded release: `2.1.0`).

## Highlights since `2.1.0`

- `iconize_sync` reads now pass `--no-optional-locks` to avoid lock contention with concurrent Dropbox-synced commits.
- `lazy-obsidian.install` Step 1 clarified — `installed_plugins.json` `projectPath` is no longer used to gate the install; only an absent or empty entry array aborts.
- `lazy-obsidian.install` resolves `lazycortex-core`'s `default-tiers.json` via `$LAZYCORTEX_PLUGIN_DIRS` first, falling back to the plugin cache only when the env is unset. Consistent with every other `lazy-*.install` skill, per the inter-plugin boundary contract.

---
*Frontmatter + status header generated by the publish-status agent. The `## Highlights since …` body is curated by the pre-commit pipeline on each meaningful commit; treat it as LLM-maintained narrative.*
