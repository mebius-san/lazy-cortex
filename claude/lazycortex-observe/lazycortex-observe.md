---
role: plugin-status
stage: changed
plugin: lazycortex-observe
current_version: 0.6.0
published_version: 0.5.0
generated_at: 2026-07-14 16:07:29 UTC
iconize_icon: LiFileClock
iconize_color: "#fde68a"
---
# lazycortex-observe — changed

**Status**: changed — version `0.6.0` is ahead of published `0.5.0`; unpublished work is pending the next `/pub.publish` run.

## Highlights since 0.5.0

- The runtime dashboard now shows when a daemon is alive but deliberately idle: a Work column in the per-daemon table reads PAUSED while the daemon skips dispatch over a dirty working tree (paired with the core's new `dirty_tree` gauge). The token table and donut gain the expert dimension (with core ≥ 5.15.0), model names drop the `claude-` prefix, staleness columns are retired, the middle row is tighter, donuts sit two-by-two, and legends are consistent — bottom on line charts, right on donuts.

---
*Frontmatter + status header generated by the publish-status agent. The `## Highlights since …` body is curated by the pre-commit pipeline on each meaningful commit; treat it as LLM-maintained narrative.*
