---
role: plugin-status
stage: changed
plugin: lazycortex-observe
current_version: 1.0.0
published_version: 0.9.0
generated_at: 2026-09-02 06:56:49 UTC
iconize_icon: LiFileClock
iconize_color: "#fca5a5"
---
# lazycortex-observe — changed since publish

**Status**: changed — version `1.0.0` is ahead of published `0.9.0`; unpublished work has landed under `claude/lazycortex-observe/`.

## Highlights since 0.9.0

- The Runtime dashboard's tables were polished: the Routines table gained the same estimated-cost column the daemon and expert tables carry, the table titles dropped the word "health" (Daemons / Routines / Experts), column widths were fixed — narrow Halted, Work, and Repo columns so the Busy time gauge and the Routine and Expert name columns get the space — and the status highlights were toned down to muted translucent shades, with Deferred cells now highlighted in yellow while Failed, Dead, and Errors stay red.
- Busy time and Cost got period-aware signalling: bars are gone, values show as compact `h`/`m` numbers and dollars, and a muted translucent background fades from green through yellow and orange to red — full red at 25% of the selected period for time and at a $250-per-day rate for cost, with the thresholds recomputed for whatever period is selected. Done in the Experts table became a completion percentage, and the expert pump's token cost now lands on its real `lazy-expert.pump` row.

- Breaking: the plugin's self-audit skill (`lazy-observe.audit`) is removed from the public surface; its checks live in the maintainer's own tooling. Skill bodies now resolve their helpers through `${CLAUDE_PLUGIN_ROOT}` so they work outside the development monorepo.

- The Daemons table folds Work / Halted / Paused into a single Status column, and a paused daemon now shows as PAUSED.
---
*Frontmatter + status header generated by the publish-status agent. The `## Highlights since …` body is curated by the pre-commit pipeline on each meaningful commit; treat it as LLM-maintained narrative.*
