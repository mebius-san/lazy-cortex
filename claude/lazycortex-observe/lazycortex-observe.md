---
role: plugin-status
stage: changed
plugin: lazycortex-observe
current_version: 0.4.0
published_version: 0.3.0
generated_at: 2026-07-12 15:23:50 UTC
iconize_icon: LiFileClock
iconize_color: "#86efac"
---
# lazycortex-observe — changed

**Status**: changed — version `0.4.0` is ahead of published `0.3.0`; unpublished work is pending the next `/pub.publish` run.

## Highlights since 0.3.0

- The installer now looks before it leaps: a pre-flight step detects an already-working collection stack (running Prometheus/collector, an installed observe service, live scrape connections) and stops untouched without asking a single question, printing what it found. A new `--integrate-only` mode feeds an existing Prometheus a file_sd target list instead of installing a second collector, and `--force-standalone` remains for deliberate overrides. Rendered shipper configs now cover every runtime daemon on the host (one target per daemon), and the doctor probes each daemon's endpoint instead of assuming a single one.

---
*Frontmatter + status header generated by the publish-status agent. The `## Highlights since …` body is curated by the pre-commit pipeline on each meaningful commit; treat it as LLM-maintained narrative.*
