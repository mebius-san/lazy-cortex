---
role: plugin-status
stage: changed
plugin: lazycortex-observe
current_version: 0.9.0
published_version: 0.8.5
generated_at: 2026-08-27 11:58:43 UTC
iconize_icon: LiFileClock
iconize_color: "#fde68a"
---
# lazycortex-observe — changed since publish

**Status**: changed — version `0.9.0` is ahead of the published `0.8.5`; the pending cycle below ships with the next publish.

## Highlights since 0.8.5

- The install run now provisions the plugin's Grafana dashboards itself: a new step copies the shipped dashboard JSON into the host Grafana's provisioning tree, resolving the target from the answer file, a running `grafana server` process, or the packaged `grafana.ini` locations — no manual import, no Grafana restart, and a host without a reachable provisioning directory is a reported skip, never an error. The runtime dashboard itself grew new panels in the same pass, and the `ps` / `lsof` probes backing the detection are registered in the dev deps table.

---
*Frontmatter + status header generated by the publish-status agent. The `## Highlights since …` body is curated by the pre-commit pipeline on each meaningful commit; treat it as LLM-maintained narrative.*
