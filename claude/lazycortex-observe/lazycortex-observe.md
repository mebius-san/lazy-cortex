---
role: plugin-status
stage: patch
plugin: lazycortex-observe
current_version: 0.7.4
published_version: 0.7.2
generated_at: 2026-07-26 10:30:00 UTC
iconize_icon: LiFileClock
iconize_color: "#93c5fd"
---
# lazycortex-observe — pending changes

**Status**: changed — `plugin.json` version `0.7.4` has not been published yet (last recorded release: `0.7.2`).

## Highlights since `0.7.2`

- The runtime dashboard's tables now agree on what a number means: the Daemons Health table (renamed from Daemons, five rows visible, with its own Busy time column) counts failed/dead jobs as per-period flow exactly like Expert health, instead of mixing in the current queue stock — stock stays visible in the Queue table. The per-routine error breakdown panel is gone as redundant with the Routine health Errors column.
- Error and failure cells read at a glance: non-zero Errors / Failed / Dead values get a red cell background, bar gauges survive only on Busy time columns, count columns are half their old width, and the Daemons table headers are short (Failed, Dead).

---
*Frontmatter + status header generated by the publish-status agent. The `## Highlights since …` body is curated by the pre-commit pipeline on each meaningful commit; treat it as LLM-maintained narrative.*
