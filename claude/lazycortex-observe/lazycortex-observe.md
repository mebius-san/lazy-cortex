---
role: plugin-status
stage: changed
plugin: lazycortex-observe
current_version: 0.5.0
published_version: 0.4.0
generated_at: 2026-07-14 09:27:08 UTC
iconize_icon: LiFileClock
iconize_color: "#fde68a"
---
# lazycortex-observe — changed

**Status**: changed — version `0.5.0` is ahead of published `0.4.0`; unpublished work is pending the next `/pub.publish` run.

## Highlights since 0.4.0

- The runtime dashboard is now multi-daemon-first: the top status strip is replaced by a per-daemon table (one row per repo — liveness, halt state, failed/dead jobs and errors with gradient bars), model names drop their `claude-` prefix, the job-activity charts move up next to the queue table, and the token section closes the page with a per-repo donut added.

---
*Frontmatter + status header generated by the publish-status agent. The `## Highlights since …` body is curated by the pre-commit pipeline on each meaningful commit; treat it as LLM-maintained narrative.*
