---
role: plugin-status
stage: changed
plugin: lazycortex-observe
current_version: 0.8.3
published_version: 0.8.2
generated_at: 2026-08-09 09:24:11 UTC
iconize_icon: LiFileClock
iconize_color: "#93c5fd"
---
# lazycortex-observe — changed since publish

**Status**: changed — version `0.8.3` is ahead of the published `0.8.2`; commits have landed under `claude/lazycortex-observe/` since the last release.

## Highlights since 0.8.2

- The critical halt alert no longer tells the operator the wrong story. Its description asserted that the daemon stops scheduling because the working tree went dirty, which is only one of the conditions that halt it — a page raised by a diverged branch, a refused push, an unreachable remote, the loop detector, or a routine whose configuration stopped matching its schema arrived describing dirt that was not there, and sent the reader looking in the wrong place. The alert now explains what each reason means and where its detail lives, and states plainly that no halt ever clears itself.

---
*Frontmatter + status header generated by the publish-status agent. The `## Highlights since …` body is curated by the pre-commit pipeline on each meaningful commit; treat it as LLM-maintained narrative.*
