---
role: plugin-status
stage: patch
plugin: lazycortex-python
current_version: 1.8.1
published_version: 1.8.0
generated_at: 2026-06-27 14:40:00 UTC
iconize_icon: LiFileClock
iconize_color: "#93c5fd"
---
# lazycortex-python — patch pending

**Status**: patch pending — `1.8.1` is staged over published `1.8.0`. Drop-in for consumers: `/plugin update` delivers the new `pcf` checker transparently, no re-install needed.

## Highlights since 1.8.0

- `pcf` (the critical-fail style checker) no longer flags dunder tokens (`__name__`-style) as unresolved identifiers, and resolves the attribute-name literal of `getattr` / `setattr` / `hasattr` / `delattr` in its main analyzer pass — fewer false positives on dynamic-attribute code.

---
*Frontmatter + status header generated by the publish-status agent. The `## Highlights since …` body is curated by the pre-commit pipeline on each meaningful commit; treat it as LLM-maintained narrative.*
