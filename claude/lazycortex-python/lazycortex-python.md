---
role: plugin-status
stage: patch
plugin: lazycortex-python
current_version: 2.4.1
published_version: 2.4.0
generated_at: 2026-08-06 19:55:00 UTC
iconize_icon: LiFileClock
iconize_color: "#93c5fd"
---
# lazycortex-python — pending changes

**Status**: changed — `plugin.json` version `2.4.1` has not been published yet (last recorded release: `2.4.0`).

## Highlights since `2.4.0`

- The checkers help chapter now documents the marker-clause check that shipped in 2.4.0 — `pcf` flags an `opt:` or `limit:` whose colon ends the line, reads real comment tokens so the same text in a string literal is not a marker, and leaves whether the clause says anything real to the review phase.
- Skill and agent descriptions rewritten to open with the request that should invoke them rather than a summary of their internals.

---
*Frontmatter + status header generated by the publish-status agent. The `## Highlights since …` body is curated by the pre-commit pipeline on each meaningful commit; treat it as LLM-maintained narrative.*
