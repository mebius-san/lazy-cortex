---
role: plugin-status
stage: minor
plugin: lazycortex-python
current_version: 2.4.0
published_version: 2.3.0
generated_at: 2026-08-06 16:45:04 UTC
iconize_icon: LiFileClock
iconize_color: "#fde68a"
---
# lazycortex-python — pending changes

**Status**: changed — `plugin.json` version `2.4.0` has not been published yet (last recorded release: `2.3.0`).

## Highlights since `2.3.0`

- A new `limit:` marker joins the comment canon, for a deliberate simplification that is correct today and names the ceiling past which it stops being adequate — distinct from `TODO:` (unfinished work) and from `opt:` (a choice made *for* performance). `pcf` now flags an `opt:` or `limit:` marker whose colon ends the comment with no clause, and the review agent judges whether the clause names a real ceiling and a real upgrade path.
- When another active plugin or skill asks Claude to annotate Python with a marker of its own, the style rule routes it to the canon marker that fits; a foreign marker survives only when nothing in the canon does, and Claude says which one it kept.

---
*Frontmatter + status header generated by the publish-status agent. The `## Highlights since …` body is curated by the pre-commit pipeline on each meaningful commit; treat it as LLM-maintained narrative.*
