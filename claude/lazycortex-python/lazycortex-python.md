---
role: plugin-status
stage: changed
plugin: lazycortex-python
current_version: 4.0.1
published_version: 4.0.0
generated_at: 2026-08-22 08:00:00 UTC
iconize_icon: LiFileClock
iconize_color: "#93c5fd"
---
# lazycortex-python — changed since publish

**Status**: changed — version `4.0.1` is ahead of the published `4.0.0`; the pending cycle below ships with the next publish.

## Highlights since 4.0.0
- Every skill and agent documents its own execution order without depending on the Task checklist tool. The Execution-discipline preamble now asks the executing agent to write out a step ledger in its own output — the same canonical step list and the same one-outcome-per-step contract — and the tool entries are gone from every `allowed-tools:` / `tools:` list.
- The `bin/` workers carry their own domain documentation. `pcf.py`, `pch.py`, `review.py`, `toi.py`, and `_pytest_dedup.py` gained `Domain(…)` blocks and `Contract:` blocks for the guarantees callers already relied on.
- The domain-groups dictionary lives under the project's own `docs/guidelines/` again, so a consumer edits it where the rest of its overlay lives.
- `Domain(group):` blocks drop the optional `[tag]` bracket syntax — group name only now, and the `lazy-python.domain-writer` agent and documenting guidelines no longer mention per-block tags.

---
*Frontmatter + status header generated by the publish-status agent. The `## Highlights since …` body is curated by the pre-commit pipeline on each meaningful commit; treat it as LLM-maintained narrative.*
