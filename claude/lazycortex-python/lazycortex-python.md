---
role: plugin-status
stage: changed
plugin: lazycortex-python
current_version: 2.0.7
published_version: 2.0.6
generated_at: 2026-07-22 14:18:24 UTC
iconize_icon: LiFileClock
iconize_color: "#93c5fd"
---
# lazycortex-python — changes pending publish

**Status**: changed — version `2.0.7` is ahead of published `2.0.6`; commits have landed under `claude/lazycortex-python/` since the last publish.

## Highlights since 2.0.6

- The magic-literal check no longer flags the declared-name first argument of `ContextVar`, `TypeAliasType`, `make_dataclass`, `new_class`, and `ReprEnum` — the same validated-name exemption `TypeVar` and the enum constructors already had.

---
*Frontmatter + status header generated by the publish-status agent. The `## Highlights since …` body is curated by the pre-commit pipeline on each meaningful commit; treat it as LLM-maintained narrative.*
