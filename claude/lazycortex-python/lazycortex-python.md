---
role: plugin-status
stage: changed
plugin: lazycortex-python
current_version: 1.9.1
published_version: 1.9.0
generated_at: 2026-07-06 13:51:11 UTC
iconize_icon: LiFileClock
iconize_color: "#93c5fd"
---
# lazycortex-python — unpublished changes

**Status**: changed — `plugin.json` is at `1.9.1` while the public mirror still ships `1.9.0`. Pending publish.

## Highlights since 1.9.0

- `tst-py` now collapses tests that were collected twice through re-export shims (a `test_all.py` aggregator plus per-package shim modules), so each test runs exactly once instead of double — clearing the inflated test counts and the order-dependent failures that double collection caused. It is automatic (no setting to enable), a no-op in repos without such shims, and prints a one-line summary naming how many duplicates it removed.

---
*Frontmatter + status header generated by the publish-status agent. The `## Highlights since …` body is curated by the pre-commit pipeline on each meaningful commit; treat it as LLM-maintained narrative.*
