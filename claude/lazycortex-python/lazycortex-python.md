---
role: plugin-status
stage: changed
plugin: lazycortex-python
current_version: 1.10.0
published_version: 1.9.2
generated_at: 2026-07-08 19:39:15 UTC
iconize_icon: LiFileClock
iconize_color: "#fde68a"
---
# lazycortex-python — changed

**Status**: changed — version `1.10.0` is ahead of published `1.9.2`; unpublished work is pending the next `/pub.publish` run.

## Highlights since 1.9.2

- Module-docstring policy flipped: regular `.py` files no longer carry a module docstring — the mandate now lives solely on `__init__.py`. The coding canon's Module Structure order and the scaffold template both encode the new policy, and a dedicated `init-template.py` (glob `**/__init__.py`) scaffolds package files with the canonical package docstring.
- Install scope is now derived from where the plugin is actually enabled, via the shared `lazycortex-core detect-scope` primitive.

---
*Frontmatter + status header generated by the publish-status agent. The `## Highlights since …` body is curated by the pre-commit pipeline on each meaningful commit; treat it as LLM-maintained narrative.*
