---
role: plugin-status
stage: changed
plugin: lazycortex-python
current_version: 1.9.2
published_version: 1.9.1
generated_at: 2026-07-06 18:04:17 UTC
iconize_icon: LiFileClock
iconize_color: "#86efac"
---
# lazycortex-python — unpublished changes

**Status**: changed — `plugin.json` is at `1.9.2` while the public mirror still ships `1.9.1`. Pending publish.

## Highlights since 1.9.1

- Fixed the `tst-py` re-export dedup so it no longer drops inherited test methods: the de-duplication key is now built from the collected class rather than the test function. An inherited method is one function object shared across subclasses, so the previous function-based key wrongly collapsed distinct subclasses (`TestA(Base)` and `TestB(Base)` sharing `Base.test_x`) into a single test; keying on the class keeps each subclass's inherited methods while still collapsing genuine re-export-shim duplicates.

---
*Frontmatter + status header generated by the publish-status agent. The `## Highlights since …` body is curated by the pre-commit pipeline on each meaningful commit; treat it as LLM-maintained narrative.*
