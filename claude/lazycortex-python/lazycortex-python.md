---
role: plugin-status
stage: minor
plugin: lazycortex-python
current_version: 1.7.0
published_version: 1.6.0
generated_at: 2026-06-10 21:22:41 UTC
iconize_icon: LiFileClock
iconize_color: "#fde68a"
---
# lazycortex-python — pending changes

**Status**: changed — `plugin.json` version `1.7.0` has not been published yet (last recorded release: `1.6.0`).

## Highlights since `1.6.0`

- `lazy-python.install` now asks the user nothing. The `inspect.sh` probe runs first (Step 3) and fully derives `pch`: PyCharm present → `[tool.pch]` is deployed automatically; no PyCharm → it is silently omitted — no prompt, no persisted flag. The opt-in `CLAUDE.md` discipline-pointer step is removed entirely: the plugin rules load from `.claude/rules/` regardless, so the pointer was redundant and the install never touches `CLAUDE.md`. The only `AskUserQuestion` that can still fire is a genuine File-sync conflict on a consumer-edited file.

---
*Frontmatter + status header generated by the publish-status agent. The `## Highlights since …` body is curated by the pre-commit pipeline on each meaningful commit; treat it as LLM-maintained narrative.*
