---
role: plugin-status
stage: minor
plugin: lazycortex-python
current_version: 1.8.0
published_version: 1.7.1
generated_at: 2026-06-27 11:51:09 UTC
iconize_icon: LiFileClock
iconize_color: "#fde68a"
---
# lazycortex-python — minor pending

**Status**: minor pending — `1.8.0` is staged over published `1.7.1`. Consumers re-run `/lazy-python.install` to pick up the rebuilt `chk-py` / `tst-py` wrappers; a bare `/plugin update` refreshes the templates but does not redeploy the wrappers.

## Highlights since 1.7.1

- `chk-py` / `tst-py` now locate the plugin at run time — from Claude Code's install manifest, with a dev-source and daemon fallback — instead of a path frozen into the wrapper at install. They keep working across plugin version bumps without re-running install, and no longer bake an absolute home path into the tracked `cli/` wrappers.

---
*Frontmatter + status header generated by the publish-status agent. The `## Highlights since …` body is curated by the pre-commit pipeline on each meaningful commit; treat it as LLM-maintained narrative.*
