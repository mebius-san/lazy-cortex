---
role: plugin-status
stage: changed
plugin: lazycortex-python
current_version: 2.0.3
published_version: 2.0.2
generated_at: 2026-07-18 16:24:53 UTC
iconize_icon: LiFileClock
iconize_color: "#93c5fd"
---
# lazycortex-python — changed since publish

**Status**: changed — version `2.0.3` is ahead of published `2.0.2`; unpublished commits under `claude/lazycortex-python/`.

## Highlights since 2.0.2

- The `toi` type-only-import checker now honours `# waiver: <reason>` comments: a waiver above (or inline to the right of) an import silences a false positive for a name a runtime library resolves from annotations — the whole block via the `from … import (` header, or a single name via its own line — while a reasonless `# waiver:` never suppresses, and `-v` reports the waived-finding count.

---
*Frontmatter + status header generated by the publish-status agent. The `## Highlights since …` body is curated by the pre-commit pipeline on each meaningful commit; treat it as LLM-maintained narrative.*
