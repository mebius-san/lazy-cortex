---
role: plugin-status
stage: major
plugin: lazycortex-python
current_version: 2.0.0
published_version: 1.10.0
generated_at: 2026-07-13 21:21:41 UTC
iconize_icon: LiFileClock
iconize_color: "#fca5a5"
---
# lazycortex-python — pending changes

**Status**: changed — `plugin.json` version `2.0.0` has not been published yet (last recorded release: `1.10.0`).

## Highlights since `1.10.0`

- **Breaking:** pcf's docstring-section and field-name defaults are now project-neutral — the `Generation Rules` / `Value Ranges` sections and the `_field_filters` escape hatch no longer ship built-in; consumers relying on them must declare `[tool.pcf] extra_docstring_sections`, `d2_exempt_marker_attrs`, and `private_name_allowlist` in their own `pyproject.toml` (commented examples in the shipped pyproject template).
- pcf accepts consumer-registered docstring sections (name, list style, order anchor, `ref_exempt` shield for `# REF:`-carrying bodies) and merges them into the section-order, style, and D5/D7/D9 machinery at load.
- The documenting canon and the docstring-writer agent were rewritten project-neutral: project-specific section rules now live in the consumer overlay, and the D2 escape hatch is described generically.
- Consumer-registered `definition`-style sections are skipped by the D9 private-name narrative check, matching the built-in definition sections; anchor chaining, malformed-entry tolerance, and the D5/D9 ref-exempt shield are pinned by tests.

---
*Frontmatter + status header generated by the publish-status agent. The `## Highlights since …` body is curated by the pre-commit pipeline on each meaningful commit; treat it as LLM-maintained narrative.*
