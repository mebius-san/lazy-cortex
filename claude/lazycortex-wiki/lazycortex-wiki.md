---
role: plugin-status
stage: changed
plugin: lazycortex-wiki
current_version: 2.3.0
published_version: 2.2.1
generated_at: 2026-09-06 19:16:00 UTC
iconize_icon: LiFileClock
iconize_color: "#fde68a"
---
# lazycortex-wiki — changed since publish

**Status**: changed — version `2.3.0` is ahead of published `2.2.1`; unpublished commits sit under `claude/lazycortex-wiki/`.

## Highlights since 2.2.1

- `mirror-sync`, curation dispatch, and axes writes resolve `lazycortex-core` from the plugin cache the plugin runs from on a consumer install, with cached versions compared numerically.
- Every question `lazy-wiki.*` skills raise now prints a context block first (which step, what was found, why it asks, what each answer does now and later) and names its target; a dead post-install line in `lazy-wiki.install` step 8 is gone.
- `/lazy-wiki.configure` no longer asks what it can derive: the review-skip filter is always seeded, a new `wiki.domains` takes the shipped dictionary path and output directory, a scope covering a spec catalog gets its excludes from `/lazy-spec.install`, and the exclude question never proposes candidates the agent invented.

---
*Frontmatter + status header generated by the publish-status agent. The `## Highlights since …` body is curated by the pre-commit pipeline on each meaningful commit; treat it as LLM-maintained narrative.*
