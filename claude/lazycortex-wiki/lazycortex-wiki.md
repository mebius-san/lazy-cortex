---
role: plugin-status
stage: patch
plugin: lazycortex-wiki
current_version: 1.2.0
published_version: —
generated_at: 2026-05-29 11:41:08 UTC
iconize_icon: LiFileClock
iconize_color: "#86efac"
---
# lazycortex-wiki — pending changes

**Status**: changed — `plugin.json` version `1.2.0` has not been published yet (last recorded release: `—`).

## Highlights since `—`

- **Daemon-free relink** — `/wiki.relink` curates a scope in-session via a thin curator subagent (no runtime daemon needed): initial full pass or incremental delta since the last run, anchored by `wiki_synced_sha` with a per-node `wiki_src_hash` content backstop when git history is rewritten.
- **Connectors + candidates** — classify now emits `wiki_connectors` (linkable-facet phrases beyond the one-line summary, surfaced in `topics.md`); the link phase accepts a pre-filtered `candidates` channel.
- **Candidate recall** — `find-candidates` pre-filters link targets before the curator judges (retrieve-then-rerank): Layer 1 (shared topic tags + `wiki_connectors`/summary overlap) bootstraps from the first pass; Layer 2 (Adamic-Adar + Personalized PageRank over the See-also graph) is scaffolded for incremental enrichment. `/wiki.relink` now feeds the curator this shortlist.

---
*Frontmatter + status header generated by the publish-status agent. The `## Highlights since …` body is curated by the pre-commit pipeline on each meaningful commit; treat it as LLM-maintained narrative.*
