---
role: plugin-status
stage: changed
plugin: lazycortex-review
current_version: 6.3.1
published_version: 6.3.0
generated_at: 2026-09-04 UTC
iconize_icon: LiFileClock
iconize_color: "#93c5fd"
---
# lazycortex-review — changed since publish

**Status**: changed — version `6.3.1` is ahead of published `6.3.0`; unpublished commits sit under `claude/lazycortex-review/`.

## Highlights since 6.3.0

- Coordination playbook's History discipline now requires naming operator removals explicitly ("the document no longer says X") instead of folding them into change counts, so writers can honor removed content as a one-way door; a finished command now folds into one content line as well — the mini-plan with its step marks dies with the callout and internal dispatch steps never enter History.
- The minutely collect sweep now closes writer rounds that finished with nothing to change: an `empty` outcome raises the job-done wake and is consumed like an `edited` one, instead of stranding the document in "Waiting: writer" until the daily sanitizer.
- The History section's explainer line is seeded as an HTML comment instead of italic text (legacy italic explainers migrate on the next review start), and the playbook now requires a History line on every landed round — the opening round included.

---
*Frontmatter + status header generated by the publish-status agent. The `## Highlights since …` body is curated by the pre-commit pipeline on each meaningful commit; treat it as LLM-maintained narrative.*
