---
tags:
  - {{product_tag}}
  - spec/empty
spec_role: vision
spec_doc_type: vision
wiki_pinned_topics:
  - wiki/doc-kind/vision
  - wiki/product/{{product}}
  - wiki/category/{{category}}
spec_stage: empty
spec_source_requests: []
spec_source_docs: []
---
# {{slug}} — vision
<!-- Why the asset exists and what it must achieve: its purpose, the goals it pursues, the obligations it takes on, and the value it gives its users. It states intent and never the solution. How the asset is shaped lives in the sibling design. -->

## Overview
<!-- The asset's purpose: who it serves, what it changes for them, and what their world looks like once the asset exists. A few sentences of prose, no lists. Goals and obligations live in their own sections, and the solution lives in the sibling design. -->

## Goals
<!-- The states of affairs the operator wants the asset to bring about. One bullet per goal: the state, then how its achievement is measured. What the system must do to get there lives in Requirements. -->

## Requirements
<!-- The obligations the system takes on so that the goals come about. One bullet per obligation in the form [condition] the system [action] [object] [constraint]; each is necessary, singular, verifiable and free of implementation. A goal restated with the system as its subject is still a goal and belongs in Goals. -->

## Value Proposition
<!-- Optional. When the document has nothing to say here, the section is removed together with its heading. What each group of users gets from the asset that it cannot get without it. One bullet per group: the group, then the value it receives. Features and their mechanics live in the sibling design. -->

## Design Concept
<!-- Optional. When the document has nothing to say here, the section is removed together with its heading. The few fundamental decisions that shape the solution, each with its reason. One bullet per decision. The detailed structure lives in the sibling design. -->

# Sources
#protected/spec/sources

## Requests
<!-- auto:spec-requests:start -->
<!-- auto:spec-requests:end -->

## Docs
<!-- auto:spec-docs:start -->
<!-- auto:spec-docs:end -->
