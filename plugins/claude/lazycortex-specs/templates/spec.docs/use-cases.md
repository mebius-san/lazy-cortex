---
tags:
  - {{product_tag}}
  - spec/empty
spec_role: use-cases
spec_doc_type: use-cases
wiki_pinned_topics:
  - wiki/doc-kind/use-cases
  - wiki/product/{{product}}
  - wiki/category/{{category}}
spec_stage: empty
spec_source_requests: []
spec_source_docs: []
---
# {{slug}} — use-cases
<!-- What the users need from the asset, in their own language: who acts, what each is trying to accomplish, and the flows that get them there. It is written before any design starts and names no system internals. How the asset is built lives in the sibling design. -->

## Terms
<!-- Optional. When the document has nothing to say here, the section is removed together with its heading. The terms this document uses, with their meaning. One line per term, alphabetical; a term the repository's dictionary already defines carries that definition verbatim, and a term this document introduces is defined here first. A term the document does not use does not belong here. -->

## Actors
<!-- The people, systems or devices that interact with the asset, each in a role. One bullet per actor: the role and what it wants from the asset. What each actor does lives in Use cases. -->

## Use cases
<!-- The scenarios in which an actor reaches a goal. One `UC-<n>` block per use case in the shape below: actor, goal, preconditions, main flow, alternative flows, postconditions. What is deliberately not delivered lives in Out of scope. -->

### UC-1 — _<name>_
**Actor**: _who initiates this._
**Goal**: _what the actor is trying to accomplish._
**Preconditions**: _what must already be true before this can start._
**Main flow**: _the steps that lead to the goal, in order._
**Alternative flows**: _branches off the main flow — errors, choices, edge paths — and where each rejoins or ends._
**Postconditions**: _what is true once this use case completes._

## Out of scope
<!-- Optional. When the document has nothing to say here, the section is removed together with its heading. The functions the asset deliberately does not deliver. One bullet per function, naming who owns it instead. A function it does deliver lives in Use cases. -->

# Sources
#protected/spec/sources

## Requests
<!-- auto:spec-requests:start -->
<!-- auto:spec-requests:end -->

## Docs
<!-- auto:spec-docs:start -->
<!-- auto:spec-docs:end -->
