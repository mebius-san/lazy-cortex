---
tags:
  - {{product_tag}}
  - spec/draft
spec_role: bug
spec_doc_type: bug
wiki_pinned_topics:
  - wiki/doc-kind/bug
  - wiki/product/{{product}}
  - wiki/category/{{category}}
spec_stage: draft
spec_source_requests: []
spec_source_docs: []
---
# {{slug}} — bug
<!-- The defect report: what happens, how to make it happen, what should happen instead, and where it shows. It is written by whoever met the defect and is what the fix's plan is judged against. The fix itself lives in the sibling code-plan. -->

## Overview
<!-- The occurrence, nature and status of the defect. A few sentences of prose. The steps that trigger it live in Way to reproduce. -->

## Way to reproduce
<!-- The steps that trigger the symptom. A numbered list, each step an exact input, URL, click or command. What the steps produce lives in Observed behavior. -->

1. _First concrete step — exact input, URL, click, or command._
2. _Next step._
3. _Final step that triggers the symptom._

## Observed behavior
<!-- The behaviour produced when the steps are followed. A few sentences, with the exact output or message. What should happen instead lives in Expected behavior. -->

## Expected behavior
<!-- The behaviour predicted under the same steps, based on the spec or the previous release. A few sentences, one outcome per sentence; the fix's plan copies them as its goals. What actually happens lives in Observed behavior. -->

## Environment
<!-- Optional. When the document has nothing to say here, the section is removed together with its heading. Where the bug shows: platform, version, configuration, data. One bullet per element of the environment. A code location or log lives in Related code / logs. -->

## Related code / logs
<!-- Optional. When the document has nothing to say here, the section is removed together with its heading. The code locations, log excerpts and traces that point at the cause. One bullet per location, as a forge URL made with `lazy-spec.source-url`; a source URL is permitted in this section only. The environment the bug shows in lives in Environment. -->

# Sources
#protected/spec/sources

## Requests
<!-- auto:spec-requests:start -->
<!-- auto:spec-requests:end -->

## Docs
<!-- auto:spec-docs:start -->
<!-- auto:spec-docs:end -->
