---
tags:
  - {{product_tag}}
  - spec/draft
product: {{product}}
spec_role: use-cases
spec_doc_type: system-use-cases
wiki_pinned_topics:
  - wiki/doc-kind/use-cases
  - wiki/product/{{product}}
spec_stage: draft
spec_source_requests: []
spec_source_docs: []
---
# {{product}} — use-cases
<!-- What the users need from the product as a whole, in their own language: who acts, what each is trying to accomplish, and the flows that cross its assets to get them there. It answers the sibling vision, is written before the sibling design, and names no system internals. A single asset's use cases live in that asset's own use-cases, which refines this document. -->

## Terms
<!-- Optional. When the document has nothing to say here, the section is removed together with its heading. The terms this document uses, with their meaning. One line per term, alphabetical; a term the repository's dictionary already defines carries that definition verbatim, and a term this document introduces is defined here first. A term the document does not use does not belong here. -->

## Actors
<!-- The people, systems or devices that interact with the product, each in a role. One bullet per actor: the role and what it wants from the product. What each actor does lives in Use cases. -->

## Use cases
<!-- The scenarios in which an actor reaches a goal through the product, at the product's own altitude. One `UC-<n>` block per use case in the shape below: actor, goal, preconditions, main flow, alternative flows, postconditions. A flow that lives entirely inside one asset belongs to that asset's use-cases; what is deliberately not delivered lives in Out of scope. -->

### UC-1 — _<name>_
**Actor**: _who initiates this._
**Goal**: _what the actor is trying to accomplish._
**Preconditions**: _what must already be true before this can start._
**Main flow**: _the steps that lead to the goal, in order._
**Alternative flows**: _branches off the main flow — errors, choices, edge paths — and where each rejoins or ends._
**Postconditions**: _what is true once this use case completes._

## Out of scope
<!-- Optional. When the document has nothing to say here, the section is removed together with its heading. The functions the product deliberately does not deliver. One bullet per function, naming who owns it instead. A function it does deliver lives in Use cases. -->

# Sources
#protected/spec/sources

## Requests
<!-- auto:spec-requests:start -->
<!-- auto:spec-requests:end -->

## Docs
<!-- auto:spec-docs:start -->
<!-- auto:spec-docs:end -->
