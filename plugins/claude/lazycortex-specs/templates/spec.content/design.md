---
tags:
  - {{product_tag}}
  - spec/draft
spec_role: design
spec_doc_type: design
wiki_pinned_topics:
  - wiki/doc-kind/design
  - wiki/product/{{product}}
  - wiki/category/{{category}}
spec_stage: draft
spec_source_requests: []
spec_source_docs: []
---
# {{slug}} — design
<!-- What this record is and which values it carries: the schema it is written against, the grounds for its values, and the records it points at. It answers the sibling vision when one exists and does not restate it. The record's data files live in the product's own repository, not here. -->

## Overview
<!-- What record this is, which schema of the project it is written against, and where in the data it lives. A few sentences of prose that open with a link to the sibling vision when one exists. Why the record exists and its goals live in the vision and are not restated here. -->

## Terms
<!-- Optional. When the document has nothing to say here, the section is removed together with its heading. The terms this document uses, with their meaning. One line per term, alphabetical; a term the repository's dictionary already defines carries that definition verbatim, and a term this document introduces is defined here first. A term the document does not use does not belong here. -->

## Design
<!-- What this record is for and why it is shaped this way. Prose, value by value, each with its grounds. The values themselves live in Definition. -->

## Definition
<!-- Every field the project's schema names for this kind of record, with its settled value. One line per field, in the schema's order; a value the design cannot settle is a decision-candidate, never a placeholder. The grounds for a value live in Design. -->

## Relations
<!-- Optional. When the document has nothing to say here, the section is removed together with its heading. The other records this one references, by the identifiers the schema uses. One bullet per record, marking which must exist before this one can be written. A field that is not a reference lives in Definition. -->

# Sources
#protected/spec/sources

## Requests
<!-- auto:spec-requests:start -->
<!-- auto:spec-requests:end -->

## Docs
<!-- auto:spec-docs:start -->
<!-- auto:spec-docs:end -->
