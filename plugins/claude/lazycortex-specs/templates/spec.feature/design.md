---
tags:
  - {{product_tag}}
  - spec/draft
spec_role: design
spec_doc_type: design
wiki_pinned_topics:
  - wiki/doc-kind/design
  - wiki/product/{{product}}
  - wiki/category/{{category}}
spec_stage: draft
spec_source_requests: []
spec_source_docs: []
---
# {{slug}} — design
<!-- What the feature is and how it behaves: its parts, the reasons behind them, the scenarios it runs through, and the frame it works within. It answers the sibling vision and does not restate it. How the code is arranged lives in the sibling architecture, and the screens in the sibling ui-design. -->

## Overview
<!-- What the feature is. A few sentences of prose that open with a link to the sibling vision. Why it exists, its goals and its value live in the vision and are not restated here. -->

## Terms
<!-- Optional. When the document has nothing to say here, the section is removed together with its heading. The terms this document uses, with their meaning. One line per term, alphabetical; a term the repository's dictionary already defines carries that definition verbatim, and a term this document introduces is defined here first. A term the document does not use does not belong here. -->

## Design
<!-- What the feature is made of and why it is shaped this way. Prose, decision by decision: the alternative chosen, the alternatives rejected, and the grounds. How the feature behaves at runtime lives in Behavior. -->

## Behavior
<!-- How the feature behaves at runtime: the scenarios its parts run through and the interactions between them. One scenario per subsection, in the order a user meets them; the flow diagram anchors here. Why the parts are shaped this way lives in Design. -->

## Constraints
<!-- Optional. When the document has nothing to say here, the section is removed together with its heading. The facts of the environment and the decisions taken elsewhere that this design works within. One bullet per constraint, naming where it comes from. The requirements the feature satisfies live in the sibling vision. -->

## Risks
<!-- Optional. When the document has nothing to say here, the section is removed together with its heading. The technical risks and the debts this design takes on. One bullet per risk, ordered by priority: the threat, then what the design does about it. A risk of the product as a whole lives in the product's design. -->

## Known Limitations
<!-- Optional. When the document has nothing to say here, the section is removed together with its heading. The properties a user might expect from the feature that it will not have. One bullet per limitation, with the reason it is left out. What the feature deliberately hands to a neighbour lives in Boundaries. -->

## Boundaries
<!-- Where the feature ends: what it deliberately does not do, and which neighbouring feature or system owns what lies beyond. One bullet per boundary, naming that neighbour. An expected property that is simply not planned lives in Known Limitations. -->

# Sources
#protected/spec/sources

## Requests
<!-- auto:spec-requests:start -->
<!-- auto:spec-requests:end -->

## Docs
<!-- auto:spec-docs:start -->
<!-- auto:spec-docs:end -->
