---
tags:
  - {{product_tag}}
  - spec/draft
spec_role: design
spec_doc_type: design
wiki_pinned_topics:
  - wiki/doc-kind/design
  - wiki/product/{{product}}
  - wiki/category/{{category}}
spec_stage: draft
spec_source_requests: []
spec_source_docs: []
---
# {{slug}} — design
<!-- What is being changed and how: the state before, the state after, and the frame the change works within. It answers the sibling vision and does not restate it. How the code is arranged lives in the sibling architecture. -->

## Overview
<!-- What is being changed. A few sentences of prose that open with a link to the sibling vision. Why it changes, its goals and its value live in the vision and are not restated here. -->

## Terms
<!-- Optional. When the document has nothing to say here, the section is removed together with its heading. The terms this document uses, with their meaning. One line per term, alphabetical; a term the repository's dictionary already defines carries that definition verbatim, and a term this document introduces is defined here first. A term the document does not use does not belong here. -->

## Current State
<!-- How it is now: the architecture, configuration or structure the change starts from, not only the behaviour. Prose, part by part. How it will be lives in Target State. -->

## Target State
<!-- How it will be once the change lands. Prose, part by part, mirroring Current State; the diagram anchors here when there is something to draw. How it is now lives in Current State. -->

## Risks
<!-- Optional. When the document has nothing to say here, the section is removed together with its heading. The threats that could sink the change. One bullet per risk: the threat, then what the design does about it. A limit the change accepts knowingly lives in Known Limitations. -->

## Known Limitations
<!-- Optional. When the document has nothing to say here, the section is removed together with its heading. What the change does only up to a known ceiling. One bullet per limitation, with the reason the ceiling is accepted. What the change deliberately does not do lives in Boundaries. -->

## Boundaries
<!-- Where the change ends: what it deliberately does not do, and which neighbouring feature owns what lies beyond. One bullet per boundary, naming that neighbour. A ceiling on what the change does do lives in Known Limitations. -->

# Sources
#protected/spec/sources

## Requests
<!-- auto:spec-requests:start -->
<!-- auto:spec-requests:end -->

## Docs
<!-- auto:spec-docs:start -->
<!-- auto:spec-docs:end -->
