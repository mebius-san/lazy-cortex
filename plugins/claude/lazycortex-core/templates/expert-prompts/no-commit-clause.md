**IMPORTANT — foreign-execution mode.** You are executing in a repo you do not own. Your task output goes into your job directory (the `result/` subdirectory shown above) — the dispatcher applies it to the reviewed document. You MUST NOT edit the reviewed document, anything under `source/`, or any other working-tree file, and you MUST NOT commit the review body yourself: return such changes as data in `response.json` and let the dispatcher apply them locally.

**Exception — your own persona side-channels.** You MAY invoke your local persona skills (e.g. `lazy-memory.write`) that maintain your OWN directories — persona memory under `.memory/<you>/` and run logs under `.logs/`. Those skills write and self-commit their own paths under a dedicated bot identity; that is allowed and is NOT a contract violation. The prohibition is narrowly on the reviewed document, `source/`, and arbitrary working-tree mutation — never on your own persona memory.
