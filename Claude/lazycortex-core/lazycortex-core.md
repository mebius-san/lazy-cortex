---
role: plugin-status
stage: major
plugin: lazycortex-core
current_version: 2.0.0
published_version: 1.3.0
generated_at: 2026-05-06 13:49:56 UTC
iconize_icon: LiFileClock
iconize_color: "#fca5a5"
---
# lazycortex-core — pending changes

**Status**: changed — `plugin.json` version `2.0.0` has not been published yet (last recorded release: `1.3.0`).

## Highlights since `1.3.0`

- Reference docs in `references/` now carry scaffold-aligned suffixes — `lazy-core.runtime` becomes `lazy-core.runtime-schema.md`, `lazy-core.scaffold-registry` becomes `lazy-core.scaffold-registry-contract.md`, `lazy-core.setup-phases` becomes `lazy-core.setup-phases-contract.md`, `expert-protocols-contract.md` gains the plugin namespace, and the `settings-v2` filename is simplified to `settings-schema.md`. Direct links must be updated by anyone hard-coding paths; the runtime resolver itself is unchanged. Every renamed file also gained the `description:` frontmatter the schema/contract templates require.
- Cleaned up seven internal markdown links the 2.0.0 rename missed (rules, audit + dispatch-job SKILLs, faq) so they point at `lazy-core.expert-protocols-contract.md`. Strengthened the `lazy-core.git` rule's stage→commit clause: do all edits in the dirty working tree first, then run `git_add` → `/pub.pre-commit` → `git_commit` back-to-back; for multi-file refactors prefer Bash `mv` over `git mv` so staging is a single late `git add -A`.

---
*Frontmatter + status header generated by `pub.status`. The `## Highlights since …` body is curated by `/pub.pre-commit` on each meaningful commit; treat it as LLM-maintained narrative.*
