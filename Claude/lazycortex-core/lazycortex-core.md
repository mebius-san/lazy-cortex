---
role: plugin-status
stage: changed
plugin: lazycortex-core
current_version: 2.0.1
published_version: 2.0.0
generated_at: 2026-05-07 12:29:16 UTC
iconize_icon: LiFileClock
iconize_color: "#86efac"
---
# lazycortex-core — pending changes

**Status**: changed — `plugin.json` version `2.0.1` has not been published yet (last recorded release: `2.0.0`).

## Highlights since 2.0.0

- `/lazy-core.checkup` now offers only consumer-relevant fix flows; author-only options and probe noise are gone.
- The doctor's always-loaded budget finding correctly identifies always-loaded rules instead of misreporting them as missing `paths:`.
- The `lazy-core.skill-writing` and `lazy-core.rule-writing` rules are tighter and consumer-only — plugin-shipping discipline (per-namespace audit/help-command contracts, plugin-vs-local rule scoping) no longer ships into consumer sessions; it lives in the dev vault now.
- Expert-runtime path is now `.experts/` everywhere it was wrong — `lazy-expert.{dispatch,collect,cancel,list}-job` skills, the protocol contract, help docs, fixtures, and the test plan no longer point at the gated `.claude/experts/` location, matching what `expert_runtime.py` and `expert_pump.py` actually use.
- `/lazy-core.install` runs a Python 3.12 floor probe as Step 0 — silent on machines that already meet the floor, and on machines that don't it walks the user through homebrew (macOS) or pyenv (cross-platform) install routes via `AskUserQuestion` and aborts cleanly so re-running the install picks up the upgrade. Per-plugin install skills inherit the gate and never re-probe.

---
*Frontmatter + status header generated by the publish-status agent. The `## Highlights since …` body is curated by the pre-commit pipeline on each meaningful commit; treat it as LLM-maintained narrative.*
