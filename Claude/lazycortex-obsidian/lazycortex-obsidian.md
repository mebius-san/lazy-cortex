---
role: plugin-status
stage: major
plugin: lazycortex-obsidian
current_version: 2.0.0
published_version: 1.1.5
generated_at: 2026-05-07 13:58:55 UTC
iconize_icon: LiFileClock
iconize_color: "#86efac"
---
# lazycortex-obsidian — pending changes

**Status**: changed — `plugin.json` version `2.0.0` has not been published yet (last recorded release: `1.1.5`, **major cycle — breaking change**). Consumers must migrate before the next install.

## Migration (1.x → 2.0)

The `gen-tag-pages` agent was renamed from `obsidian.gen-tag-pages` to `lazy-obsidian.gen-tag-pages` to match the plugin's `lazy-obsidian.*` namespace convention. Update any caller dispatch strings:

- old: `Agent(subagent_type: "lazycortex-obsidian:obsidian.gen-tag-pages", ...)`
- new: `Agent(subagent_type: "lazycortex-obsidian:lazy-obsidian.gen-tag-pages", ...)`

If you have an `agent_models` entry in `lazy.settings.json` pointing at the old name, rename the key after re-installing.

## Highlights since 1.1.5

- New `reconcile-plugin <plugin>` subcommand on the iconize-sync worker. Walks `claude/<plugin>/**/*.md` only and re-stages whatever it touches, so a plugin's folder note, README, and any other iconize-driven file repaint to the new stage color in the same commit as the bump — no more stale colors lingering until the next full vault reconcile.
- Plugin's dev test suite no longer ships in the public marketplace mirror. The `bin/tests/` subtree was relocated to the canonical `tests/obsidian/iconize-sync/` location at the repo root, removing 13 dev-only scripts and their fixtures from consumer caches.
- **Breaking:** `gen-tag-pages` agent renamed to `lazy-obsidian.gen-tag-pages` (was `obsidian.gen-tag-pages`). See the Migration section above.

---
*Frontmatter + status header generated by the publish-status agent. The `## Highlights since …` body is curated by the pre-commit pipeline on each meaningful commit; treat it as LLM-maintained narrative.*
