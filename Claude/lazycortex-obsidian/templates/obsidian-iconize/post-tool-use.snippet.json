{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "# HOOK_VERSION: 1.0.0\njq -r '.tool_response.filePath // .tool_input.file_path' | { read -r f; case \"$f\" in *.md) python3 \"{{PLUGIN_BIN_PATH}}/iconize_sync.py\" sync \"$f\" 2>/dev/null || true ;; esac; }"
          }
        ]
      }
    ]
  }
}
